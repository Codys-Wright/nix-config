# I'm empty and in plain text right now
# but I will contain soon be encrypted with all the secrets!
# 
# To encrypt this file, use:
#   just sops-edit template
#
# Or manually:
#   SOPS_AGE_KEY_FILE=sops.key nix develop --command sops --config sops.yaml -i secrets.yaml
#
# Example structure (adjust for your deployment needs):
# This file contains ONLY host-specific system secrets for this host.
# User-specific secrets (passwords, git credentials, API keys, etc.) should go in:
#   users/<username>/secrets.yaml
#
<hostname>:
  system:
    sshPrivateKey: |
      -----BEGIN OPENSSH PRIVATE KEY-----
      b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
      ...
      -----END OPENSSH PRIVATE KEY-----
    

