# Formatters configuration
# Returns config.vim settings directly
# Takes lib as parameter for consistency (even if not used)
{lib, ...}: {
      # Formatter configuration
      # Example: conform, prettier, biome, etc.
}
